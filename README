An example of a Python+Flask-based RESTful web service, using a sqlLite db

Intended for use with virtualenv

    virtualenv venv
    pip install Flask

After this initial installation step, run the following each time you use the app

    . venv/bin/activate
    python start.py
